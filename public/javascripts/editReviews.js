const editReviewBtns = document.querySelectorAll('.editReview');
const editReviewForms = document.querySelectorAll('.editReviewForm');

editReviewBtns.forEach(editBtn => {
  editBtn.addEventListener('click', () => {
    console.log('chocolate time');
  });
})

